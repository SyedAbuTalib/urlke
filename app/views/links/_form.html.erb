<%= form_with(model: link, local: true) do |form| %>
  <% if link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
        <% link.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :short_link %>
    <%= form.text_field :short_link %>
  </div>

  <div class="field">
    <%= form.label :full_link %>
    <%= form.url_field :full_link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
